import React from 'react'
import Layout from '../../Components/Layout'
import instructorProps from './instructorProps'

function InstructorPage() {
  return (
    <Layout defaultProps={instructorProps} />
  )
}

export default InstructorPage